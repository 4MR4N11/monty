#include "monty.h"

/**
 * _nop - does nothing
 * @stack: pointer to the stack
 * @line_number: line number
*/

void _nop(stack_t **stack, unsigned int line_number)
{
	(void)stack;
	(void)line_number;
}
